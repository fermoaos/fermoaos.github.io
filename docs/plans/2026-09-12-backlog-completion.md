---
goal: Fermoa 남은 백로그 완주 — 필름 3라운드(남은 실험) · 개발 3건 · 실제 챗 방어선
generated: 2026-09-12T08:50:00+09:00
complexity: L
risk: medium
status: in_progress
execution_tier: manual
outcome: unreviewed
---

# Fermoa 백로그 완주 계획 (2026-09-12)

## 왜 manual tier 인가

비가역·유상 단계가 둘이다: **필름 생성 API 호출**(유료)과 **라이브 히어로 교체**(방문자에게
보인다). 나머지는 되돌릴 수 있으므로 연속 실행한다. 필름의 **육안 승인**은 내가 대신 할 수
없다 — 검증 계약의 `human_required`.

## A. 필름 3라운드 — 남은 실험 (보드 t-74cd4bca)

2026-09-05 에 **결정만 하고 생성은 중단**됐다. 결정은 이미 나 있다: v2 채택 · S1 트림
B[4-10s](`seg_01_r3.mp4` 생성 완료) · 구 S4 폴백 트림 C(`seg_04_trimC.mp4` 생성 완료).
남은 것은 API 를 쓰는 두 컷뿐이다.

| # | 단계 | 검증 |
|---|---|---|
| A1 | S3⑤ 4s 재생성 — 시드 `r3/frame3e-copy.png`. 라운드2 프롬프트에서 **두 군데만** 바꾼다: (1) 배경 간판 금지를 명시 문장으로, (2) 마지막 1s 를 "손이 알갱이로 완전히 풀려 흩어진다"로 고정 | 마지막 프레임에 손 실물 없음 · 우상단 간판 없음 (프레임 추출 육안) |
| A2 | A1 마지막 프레임 추출 → S4 6s 재생성 (알갱이 → 사무실 복귀) | 클립 내부 밝기 점프 없음 |
| A3 | `film/assemble.sh` S1=seg_01_r3 · S3E=새것 · S4=새것 → draft v3 | 길이 ≈ 40s (ffprobe) |
| A4 | 컨택트 시트 + mp4 를 사용자에게 전달 | ⛔ **여기서 멈춘다** — 육안 승인 없이 라이브 교체 금지 |
| A5 | (승인 후) `build_film_assets.py` TRIM_S=0 → `site/assets/film/hero.mp4` → 게이트 → push | 게이트 5종 + 라이브 |

⛔ 폴백: A1 을 두 번 시도해도 간판·굳은 손을 못 고치면 **구 S4 폴백 트림 C**(이미 있음)로
간다. 세 번째 시도는 하지 않는다 — 같은 프롬프트 축을 세 번 흔드는 것은 수렴이 아니라 낭비다.

## B. 개발 — 결정 불요

| # | 항목 | 왜 지금 |
|---|---|---|
| B1 | `<head>` 6벌 → `doc_head()` 통합 | 지난 회차에 "여섯 번째가 생기면 묶는다"고 적었고 지금 **정확히 6벌**이다. 게이트가 40장을 덮으므로 회귀는 코드가 잡는다 |
| B2 | `.page-hero` 죽은 패딩 규칙 | 의도 74.9px 인데 실제 124.8px — `section:not(.hero):not(.manifesto)`(0,2,1)가 `.page-hero`(0,1,0)를 이긴다. 특이도를 맞추고 **32장 여백 변화를 측정으로 확인** |
| B3 | Careers 페이지 | ⛔ **짓지 않고 백로그에서 내린다.** 공고 내용이 없다 — 없는 채용을 페이지로 만들면 그 페이지가 곧 거짓이다 |
| B4 | 보드 `t-0eef43ae`(X박스 → 실제 미디어) | 배포본에 `xbox` **0건** — 2026-09-04 에 끝났다. 닫는다 |

## C. 실제 챗(2안) 방어선 (보드 t-0270fb5a)

호스팅·비용은 사용자 결정이다. 내가 할 수 있는 것은 **결정 없이도 만들 수 있는 방어선**이고,
그게 없으면 승인이 나도 켤 수 없다. 결정 항목은 **결정으로 분리**해 올린다(작업으로 위장 금지).

## D. 사용자 결정 대기 (건드리지 않음)

- KIPRIS 9/42류 조회 + 도메인 등록(`t-9fe1b49e`) — 브라우저·결제라 사용자 몫
- 실제 챗 호스팅 착수 — C 의 결정에 답이 온 뒤

## 검증 계약

| 산출물 | kind | 게이트 |
|---|---|---|
| 필름 | generated-video | ⛔ human_required — 컨택트 시트 육안 |
| 사이트 코드 | code-change | meta · link · a11y · dock · static · 빌드 멱등 |
